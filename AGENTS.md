# 项目级 UI 设计补充规则

## UI 比例与还原

- 还原参考 UI 时，禁止直接按截图像素反推 CSS 尺寸；Retina 截图、浏览器缩放、窗口 DPR 会把实际 CSS px 放大，必须优先查源码 token、Figma 标注或 DevTools 计算值。
- 桌面工具类/macOS 风格应用默认使用紧凑密度：根字号优先 `13px`，常规正文 `12-14px`，侧边栏/列表标题默认不超过 `15px`，`24px+` 只允许用于真正的页面标题或品牌展示，禁止用于导航、搜索框、列表正文。
- 在 `980x640` 级别窗口内，侧栏宽度默认不超过窗口宽度 `28%`，常规工具栏高度不超过 `56px`，列表行高度默认 `60-72px`；除非参考源码明确更大，否则不得自行放大。
- macOS titlebar 只能选择一种来源：原生 overlay 或前端自绘交通灯，禁止同时渲染系统交通灯和前端交通灯。
- UI 完成后必须用同窗口尺寸、同浏览器缩放/DPR 截图对比参考图；发现字体、padding、margin、控件高度整体偏大时，优先回查 token 和 CSS px。
