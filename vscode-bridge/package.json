{
  "name": "codex-account-switcher-bridge",
  "displayName": "Codex Account Switcher Bridge",
  "description": "Receives Codex Account Switcher refresh requests in every VS Code window and restarts extension hosts.",
  "version": "0.2.0",
  "publisher": "cuihongran",
  "engines": {
    "vscode": "^1.96.0"
  },
  "categories": ["Other"],
  "extensionKind": ["ui"],
  "activationEvents": ["onStartupFinished", "onUri"],
  "main": "./extension.js",
  "contributes": {
    "commands": [
      {
        "command": "codexAccountSwitcher.restartExtensionHost",
        "title": "Codex Account Switcher: Restart Extension Host"
      }
    ]
  }
}
